#include<bits/stdc++.h>
using namespace std;
class listnode{
    public:
    int val;
    listnode*next;
    listnode(int x){
        val=x;
        next=null ptr;
    }
};
listnode*insertathead(listnode*head,int value ){
       listnode*newnode=new listnode(value);
       newnode->next=
    }
}
int main(){
    
    return 0;
}